# Copy to .env and replace; scripts/init-env.ps1 generates a random local password.
POSTGRES_PASSWORD=replace-with-a-long-random-password
# Optional ChessLens heuristic thresholds; must be strictly increasing.
ANALYSIS_INACCURACY_CP=50
ANALYSIS_MISTAKE_CP=100
ANALYSIS_BLUNDER_CP=200
